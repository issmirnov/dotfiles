## regular bashrc here




## source dotfile stuffs



## Source all files in ~/.dotfiles/bash/
function src() {
  local file
  if [[ "$1" ]]; then
    source "$HOME/.dotfiles/bash/$1.sh"
  else
    for file in ~/.dotfiles/bash/*; do
      source "$file"
    done
  fi
}


# do the sourcing
src
